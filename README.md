# ReadIT

## BCDS 투자유치 랜딩페이지

`index.html` — BCDS 투자유치(IR)용 원페이지 랜딩입니다.

- **디자인**: 화이트 베이스 + 레드 포인트, 심플 & 모던, 리퀴드 글래스(글래스모피즘)
- **3D**: 외부 라이브러리 없이 순수 WebGL 셰이더로 구현한 리퀴드 글래스 블롭 (마우스 인터랙션 반응)
- **인터랙션**: 스크롤 리빌, 카운터 애니메이션, 3D 틸트 카드, 마그네틱 버튼, 커스텀 커서
- **의존성 없음**: 정적 파일만으로 동작 (폰트만 CDN 사용, 미로드 시 시스템 폰트 폴백)
- **반응형**: 모바일/태블릿/데스크톱 대응, `prefers-reduced-motion` 지원

### 실행

```bash
# 브라우저에서 index.html을 바로 열거나
python3 -m http.server 8000  # http://localhost:8000
```

### 콘텐츠 수정 안내

지표·시장 규모·팀 소개·연락처(`ir@bcds.co.kr`)는 **샘플 콘텐츠**입니다.
실제 IR 수치와 회사 정보로 교체 후 사용하세요. 모든 텍스트는 `index.html`에서 직접 수정할 수 있습니다.
